# This should be updated with custom passwords for your situation and encrypted using ansible vault or other
# tool to secure the secrets. For convenience you may set the path to the vault password file as an environment
# variable as so:
#
# export ANSIBLE_VAULT_PASSWORD_FILE=~/.my_vault_password_file
#
mysql_core_password: p@ssword
agave_core_smtps_password: password
mongodb_password: changeme
