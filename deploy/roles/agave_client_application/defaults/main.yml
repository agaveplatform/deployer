---
# defaults file for roles/agave_client_application

# the callbackUrl for oauth redirects back to the client.
# leave blank if this is a cli client applciation
agave_client_application_callback_url:

# name of the client. This will be presented on the OAuth login page.
agave_client_application_name: ""

agave_client_application_description: "Auto-generated {{tenant_id}} client application generated by Agave Deployer on {{ '%Y-%m-%d %H:%M:%S' | strftime }}"

agave_client_application_cache_dir:

apim_admin_pass: a2m1np@ss