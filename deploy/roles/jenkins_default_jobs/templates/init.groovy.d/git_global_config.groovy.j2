import jenkins.model.*

def inst = Jenkins.getInstance()

def desc = inst.getDescriptor("hudson.plugins.git.GitSCM")

desc.setGlobalConfigName("{{ jenkins_host_git_config_name }}")
desc.setGlobalConfigEmail("{{ jenkins_host_git_config_email }}")

desc.save()